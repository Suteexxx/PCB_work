# --- Web framework ---
fastapi==0.138.0
uvicorn[standard]==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.2
python-multipart==0.0.32

# --- Web search & scraping ---
ddgs==9.14.4
httpx==0.28.1
trafilatura==2.1.0
beautifulsoup4==4.14.3
lxml==6.1.1

# --- NLP / Embeddings / Keywords ---
sentence-transformers==5.6.0
keybert==0.9.0
nltk==3.9.4
scikit-learn==1.8.0

# --- Vector DB ---
chromadb==1.5.9

# --- PDF parsing (for knowledge base ingestion) ---
pypdf==5.9.0
reportlab==4.4.10

# --- Utilities ---
loguru==0.7.3
tenacity==9.1.4
python-dotenv==1.2.2
aiofiles==25.1.0

# NOTE: versions above were pinned against what actually resolved cleanly
# at build time. If you hit resolver conflicts later, loosen these to
# '>=' and let pip resolve freely, then re-pin with `pip freeze`.
